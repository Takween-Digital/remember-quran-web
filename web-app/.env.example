# Signing key for the "AuthToken" session cookie (next-firebase-auth-edge).
# Required — there is no hardcoded fallback. Generate with `openssl rand -hex 32`.
COOKIE_SECRET_CURRENT=
EMAIL_FROM=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_PROJECT_ID=
FIREBASE_WEB_API_KEY=
RESEND_API_KEY=
